{
  "name": "roomierent-json-server",
  "version": "1.0.0",
  "description": "Backend API for RoomieRent using json-server",
  "main": "server.js",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
